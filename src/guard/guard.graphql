input LoginUserInput {
  email: String,
  password: String,
}

type Guard{
  token: String,
  email: String,
  firstName: String,
  typeUser: String,
}

type Query {
  signIn(loginUser:LoginUserInput): Guard
}
